<div class="container">
  <div class="row">
    <div class="col-sm-12 mt-50">
      <div class="panel panel-primary">
      {{#each pp}}
       <div class="panel-heading"><h3>{{this.blocked}}</h3></div>
        <div class="panel-body"><img src="uploads{{this.path}}" class="img-responsive" style= "width:350px" alt="Image"></div>
                <div class="panel-footer">
                </div>
                <form action = "/block" method = "POST">
                <input type = "hidden" name = "user" value = "{{this.blocked}}">
                <input type = "hidden" name = "act" value = "unblock">
               <button type = "submit" name = "submit" class="btn btn-lg btn-danger" >unblock</button>
               </form>

        {{/each}}
      </div>
    </div>
    </div>
    </div>